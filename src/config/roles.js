// Roles must be managed by Firebase custom claims in production.
// Keep these exports for backward compatibility with older UI code.
export const SENIOR_PASTOR_EMAIL = '';
export const ADMIN_EMAILS = [];
